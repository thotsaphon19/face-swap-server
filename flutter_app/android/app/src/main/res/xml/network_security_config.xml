<?xml version="1.0" encoding="utf-8"?>
<!--
  Release (and default) network security config.
  - Allows cleartext (HTTP/WS) traffic for LAN streaming use cases.
  - Trusts only system CAs in production (plus user CAs in debug overrides).
  - The debug-overrides block additionally trusts user-installed CAs.
  - Debug builds still override this file via src/debug/res/xml/network_security_config.xml.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
