<?xml version="1.0" encoding="utf-8"?>
<!--
  Release (and default) network security config.
  - Keeps release builds secure by disallowing cleartext (HTTP/WS) traffic.
  - Trusts only system CAs in production (plus user CAs in debug overrides).
  - Debug builds override this file via src/debug/res/xml/network_security_config.xml
    to allow local HTTP/WS testing.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
