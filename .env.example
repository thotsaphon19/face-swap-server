SECRET_TOKEN=testing123
WORKER_PORT=8000
MODEL_DIR=/workspace/model/checkpoints
LOG_LEVEL=info

# Comma-separated allowed CORS origins (use * to allow all)
CORS_ORIGINS=*

# Flutter client backend URL (set this on the mobile device / in the app)
# BACKEND_URL=http://168.144.110.89
# Runpod GPU pod endpoint (used by DigitalOcean control plane if proxying)
# RUNPOD_URL=http://YOUR_RUNPOD_IP:8000